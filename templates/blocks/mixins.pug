mixin addMenuItem(pageID, item, index)
  if pageID === index
    li.page-header__menu-item.page-header__menu-item--active
      a.page-header__menu-link(href=item.url)= item.name

  else
    li.page-header__menu-item
      a.page-header__menu-link(href=item.url)= item.name
